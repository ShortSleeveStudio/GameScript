/**
 * Panel components for the GameScript UI.
 *
 * These are the main view panels displayed in Golden Layout within
 * the single GameScript webview panel.
 */

export { default as ConversationFinder } from './ConversationFinder.svelte';
export { default as ActorManager } from './ActorManager.svelte';
export { default as LocaleManager } from './LocaleManager.svelte';
export { default as LocalizationEditor } from './LocalizationEditor.svelte';

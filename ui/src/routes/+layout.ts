// With hash-based routing (router.type === 'hash'), page options like
// ssr, prerender, and csr are automatically handled by SvelteKit.
// No exports needed here - hash routing is inherently client-side only.
